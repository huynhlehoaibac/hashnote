import { TextareaElementDirective } from './textarea-element.directive';

describe('TextareaElementDirective', () => {
  it('should create an instance', () => {
    const directive = new TextareaElementDirective();
    expect(directive).toBeTruthy();
  });
});
